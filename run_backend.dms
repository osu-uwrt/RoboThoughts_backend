#!/bin/bash
kill $(lsof -t -i:8080)
#kill $(lsof -t -i:5000)

export ROS_MASTER_URI=https://baycat:11311

#start web server
rosrun web_video_server web_video_server &

#start up app.py
python ./api/app.py